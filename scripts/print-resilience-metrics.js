// This script was removed per repository cleanup.
// QA: per-test metric artifacts are still written to the artifacts/ directory when
// RESILIENCE_METRICS=1 is set. Inspect those files manually or use custom tooling.
console.log('print-resilience-metrics script is stubbed (removed). Inspect artifacts/ directory manually if needed.');
